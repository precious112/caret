/**
 * Centralized product name for all user-facing webview strings.
 *
 * Use this constant instead of hardcoding "Caret" in components so the product
 * name can be changed/white-labeled in one place. Internal identifiers
 * (ClineMessage, ClineSay, ClineDefaultTool, .clineignore, proto types, etc.)
 * are NOT branding and must stay as-is.
 */
export const PRODUCT_NAME = "Caret"
